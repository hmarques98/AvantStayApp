import { FONT_SSP_600 } from '@fonts'
import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  buttonContainer: {
    borderColor: '#011B35',
    borderWidth: 2,
    paddingVertical: 10,
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  title: { color: '#011B35', fontFamily: FONT_SSP_600, fontSize: 15 },
})
export default styles
